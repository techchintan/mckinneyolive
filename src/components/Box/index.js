import styled from 'styled-components'
import {
  background,
  border,
  color,
  flexbox,
  layout,
  position,
  space,
  typography,
} from 'styled-system'

const Box = styled.div`
  ${background}
  ${border}
  ${color}
  ${flexbox}
  ${layout}
  ${position}
  ${space}
  ${typography}
`

export default Box
