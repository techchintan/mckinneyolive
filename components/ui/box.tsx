'use client'

import styled from 'styled-components'
import {
  background,
  border,
  color,
  flexbox,
  layout,
  position,
  space,
  typography,
} from 'styled-system'

const Box = styled.div<any>`
  ${background}
  ${border}
  ${color}
  ${flexbox}
  ${layout}
  ${position}
  ${space}
  ${typography}
`

export default Box
